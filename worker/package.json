{
  "name": "alawabin-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API backend for Al-Awabin Quran Academy",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
